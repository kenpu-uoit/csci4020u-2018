pi = 3.1415

function calc_area(radius, half) {
  area = pi * radius * radius
  if half > 0 {
    area = area / 2
  }
  area
}

function bang() {
  -> 999
}

radius = 10
bang()
-> calc_area(100 + radius, 1)
-> calc_area(radius, 1)
